1)
SELECT ENAME FROM EMPLOYEE

WHERE SALARY> (SELECT SALARY FROM EMPLOYEE WHERE EMPNO=8844);

2)SELECT ENAME, DEPTNO, JOB

FROM EMPLOYEE 

WHERE JOB = (SELECT JOB FROM EMPLOYEE WHERE EMPNO=8900);

3)SELECT ENAME, DEPTNO

FROM EMPLOYEE 

WHERE SALARY IN (SELECT MIN(SALARY) FROM EMPLOYEE GROUP BY DEPTNO);

4)SELECT EMPNO, ENAME, DEPTNO
FROM EMPLOYEE 

WHERE SALARY > (SELECT AVG(SALARY) FROM EMPLOYEE);

5)SELECT ENAME, EMPNO, SALARY 

FROM EMPLOYEE
WHERE MANAGER = 8566;

6)SELECT D.DEPTNO, ENAME, JOB, DNAME
 
FROM EMPLOYEE E, DEPARTMENT D

WHERE E.deptno=D.DEPTNO AND DNAME ='MARKETING';

7)
SELECT * FROM EMPLOYEE 

WHERE SALARY=5000 AND MANAGER=8566;

8)SELECT * FROM EMPLOYEE
 
WHERE EMPNO = ANY(SELECT EMPNO FROM EMPLOYEE WHERE EMPNO=8566 OR EMPNO= 8884 OR EMPNO=8521);

9)
SELECT * FROM EMPLOYEE 
WHERE SALARY BETWEEN (SELECT MIN(SALARY) FROM EMPLOYEE) AND 2500 ;

10)SELECT * FROM EMPLOYEE  
WHERE EMPNO IN (SELECT EMPNO FROM EMPLOYEE WHERE SALARY =
(SELECT MAX(SALARY) FROM EMPLOYEE WHERE SALARY < (SELECT MAX(SALARY) FROM EMPLOYEE) ));

11)SELECT * FROM EMPLOYEE 
WHERE SALARY > (SELECT AVG(SALARY) FROM EMPLOYEE) AND DEPTNO IN (SELECT DEPTNO FROM EMPLOYEE WHERE ENAME LIKE '%M%')

12)SELECT ENAME, EMPNO, JOB
FROM EMPLOYEE E, DEPARTMENT D
WHERE E.DEPTNO=D.DEPTNO  AND LOC= 'CHENNAI';

13)SELECT * FROM EMPLOYEE 
WHERE SALARY < ALL ( SELECT SALARY FROM EMPLOYEE WHERE JOB='ANALYST');

14)SELECT * FROM EMPLOYEE 
WHERE SALARY < ALL ( SELECT SALARY FROM EMPLOYEE WHERE JOB='ANALYST') AND JOB NOT IN ('ANALYST');

15)SELECT D.DEPTNO , SUM(SALARY) AS TOTAL_SALARY
FROM EMPLOYEE  E, DEPARTMENT D 
WHERE E.DEPTNO=D.DEPTNO
GROUP BY D.DEPTNO;

16)SELECT EMPNO, ENAME, SALARY ,
CASE WHEN SALARY > (SELECT AVG(SALARY) FROM EMPLOYEE) THEN 'HIGH' ELSE 'LOW' END SALARY_STATUS
FROM EMPLOYEE

17)SELECT DISTINCT D.DEPTNO
FROM EMPLOYEE E, DEPARTMENT D
WHERE D.DEPTNO=E.DEPTNO;

18)SELECT  * FROM EMPLOYEE
WHERE  SALARY IN (SELECT MAX(SALARY) FROM EMPLOYEE GROUP BY DEPTNO);

19)SELECT ENAME 
FROM EMPLOYEE E
WHERE SALARY > (SELECT (SUM(SALARY))*0.5  FROM EMPLOYEE EE WHERE E.DEPTNO=EE.DEPTNO);

20)SELECT ENAME, HIREDATE
FROM EMPLOYEE
WHERE HIREDATE > (SELECT HIREDATE FROM EMPLOYEE WHERE EMPNO=8566)


